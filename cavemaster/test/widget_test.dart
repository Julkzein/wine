import 'package:flutter_test/flutter_test.dart';

void main() {
  // Smoke tests will be added once the widget tree is stable.
  // The default counter test is replaced here because CaveMaster has
  // no counter widget.
  testWidgets('placeholder', (tester) async {
    expect(true, isTrue);
  });
}
